<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/destinations/new.css" />
<%- include('../partials/nav') %>

<main>
	<h1><%= title %></h1>
  <!-- <%# if (error)  {%> 
    <p>"Entry already added"</p>
    <%# } %>  -->
  <p>Please first ensure that the Destination is not in the dropdown</p>
    <select>
      <% destinations.forEach(function(destination) { %>
        <option><%= destination.airport %></option>
      <% }) %>
    </select>
  <form id="add-destination-form" action="/destinations" method="POST">
    <label for="name-input">Destination:</label>
    <input type="text" name="airport" id="name-input" placeholder="LAX">
    <button type="submit">Add Destination</button>
  </form>
</main>


<%- include('../partials/footer') %>